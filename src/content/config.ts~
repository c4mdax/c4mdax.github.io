import { defineCollection, z } from 'astro:content';

const posts = defineCollection({
    type: 'content',
    schema: z.object({
        title: z.string(),
        description: z.string(),
        pubDate: z.string(), // Lo dejamos como string para que sea fácil escribir la fecha
    })
});

export const collections = { posts };